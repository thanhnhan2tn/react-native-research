import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    backgroundColor: '#c91000',
    padding: 10
  },
  text: {
    flex: 1,
    textAlign: 'center',
    color: '#fff'
  }
  })

export default styles;
