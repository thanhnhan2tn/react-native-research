import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    backgroundColor: '#666',
    padding: 10,
    alignItems: 'center'
  },
  text: {
    flex: 1,
    color: '#fff',
    paddingLeft: 10
  },
  image: {
    height: 30
  }
  })

export default styles;
