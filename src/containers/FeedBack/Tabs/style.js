import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    flexDirection: 'row',
  },
  color: {
    color: '#c91000'
  },
  active: {
    borderBottomWidth: 3,
    borderBottomColor: '#c91000'
  }
});
