import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  itemFlatlist: {
    marginRight: 10,
  },
  titleItem: {
    fontSize: 14,
    marginTop: 5,
  },
});

export default styles;
