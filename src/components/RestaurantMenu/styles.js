import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapBackground: {
    flex: 1,
  },

  wrapOverlay: {
    position: 'absolute',
    top: 65,
    right: 0,
    left: 0,
    bottom: 0,
  },

  wrapBanner: {
    flex: 1,
  },

  wrapTabView: {
    flex: 2,
  },
});
export default styles;
