import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapBackground: {
    width: '100%',
    height: '100%',
  },

  wrapOverlay: {
    position: 'absolute',
    top: 0,
    right: 0,
    left: 0,
  },
});
export default styles;
