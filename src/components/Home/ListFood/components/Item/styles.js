import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapperItem: {
    margin: 5,
  },

  imgRecomend: {
    width: 90,
    height: 90,
    borderColor: '#bfbfbf',
    borderWidth: 1,
  },

  textName: {
    color: '#505050',
  },
});
export default styles;
