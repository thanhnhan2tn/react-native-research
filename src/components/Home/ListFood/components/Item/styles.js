import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapperItem: {
    margin: 5,
  },

  imgRecomend: {
    width: 90,
    height: 90,
    borderColor: 'black',
    borderWidth: 1,
  },
});
export default styles;
